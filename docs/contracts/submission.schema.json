{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Coldline Task 1.1 public answer contract. This schema validates structure, not engineering judgment.",
  "title": "Coldline Task 1.1 answers",
  "description": "Direct runtime observations submitted through the Task pull request.",
  "type": "object",
  "additionalProperties": false,
  "required": ["answers"],
  "properties": {
    "answers": {
      "description": "The only top-level answer mapping accepted by the public verifier.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "runtime_state",
        "scenario_id",
        "active_ports",
        "trace_reference",
        "observation",
        "unresolved_gap",
        "fidelity_limitation"
      ],
      "properties": {
        "runtime_state": {
          "description": "Whether the student completed the supplied runtime workflow.",
          "enum": ["ready", "blocked"]
        },
        "scenario_id": {
          "description": "The identifier printed by the supplied scenario command.",
          "type": "string",
          "minLength": 8,
          "maxLength": 100
        },
        "active_ports": {
          "description": "Ports observed in the supplied opening checkpoint.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": ["ModelProvider", "Retriever", "ObjectStore", "JobQueue", "SecretProvider"]
          }
        },
        "trace_reference": {
          "description": "One 32-character trace identifier copied from the local run.",
          "type": "string",
          "pattern": "^[0-9a-f]{32}$"
        },
        "observation": {
          "description": "What the student directly observed in the running system.",
          "type": "string",
          "minLength": 40,
          "maxLength": 1000
        },
        "unresolved_gap": {
          "description": "One important question the current evidence does not answer.",
          "type": "string",
          "minLength": 20,
          "maxLength": 1000
        },
        "fidelity_limitation": {
          "description": "What the local stack cannot prove about a managed production system.",
          "type": "string",
          "minLength": 30,
          "maxLength": 1000
        }
      }
    }
  }
}
